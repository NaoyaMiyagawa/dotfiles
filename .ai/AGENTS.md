# Global Agent Rules (Lean)

This file stays minimal on purpose. Task-specific rules live in custom skills.

Load custom skills only when relevant to the user's request.
Rely on each skill's `name` and `description` metadata for discovery and routing.
